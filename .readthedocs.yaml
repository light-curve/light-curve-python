version: 2

build:
  os: ubuntu-22.04
  tools:
    python: latest
    rust: latest
  apt_packages:
    - libceres-dev
    - libfftw3-dev
    - libgsl-dev

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: light-curve
