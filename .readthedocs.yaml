version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: latest
    rust: latest

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: light-curve
